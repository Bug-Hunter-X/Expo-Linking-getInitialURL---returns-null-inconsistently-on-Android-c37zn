This bug occurs when using Expo's `Linking` API to handle deep links on Android.  The `getInitialURL` method sometimes returns `null` even when a deep link is successfully opened. This is inconsistent and makes it difficult to handle the initial app launch with a deep link reliably.